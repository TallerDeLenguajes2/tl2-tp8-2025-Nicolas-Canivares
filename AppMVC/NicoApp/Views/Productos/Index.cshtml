@using TiendaDB
@model List<Productos>


<article class="d-flex align-items-center justify-content-between">
    <h1 class="text-light">Listado Productos</h1>
    <a class="text-decoration-none fst-normal link-light" asp-controller="Productos" asp-action="Create">
    <button class="btn btn-primary">
        Crear
    </button></a>
</article>
<table class="table table-dark table-striped table-hover table-bordered text-center align-middle mt-2">
    <thead class="thead-dark">
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Nombre</th>
            <th scope="col">Precio</th>
            <th scope="col">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Productos producto in Model)
        {
            <tr>
                <td scope="row">@producto.IdProducto</td>
                <td>@producto.Descripcion</td>
                <td>@producto.Precio</td>
                <td>
                @* <td class="flex-grow-1 d-flex align-items-center justify-content-center"> *@
                @* <td class="d-flex align-content-md-around flex-wrap"> *@
                    <button class="btn btn-info m-2">
                        <a class="text-decoration-none fst-normal link-light" asp-area="" asp-controller="Productos"
                            asp-action="Edit" asp-route-idProducto="@producto.IdProducto">Editar</a>
                    </button>
                    <button class="btn btn-danger m-2">
                        <a class="text-decoration-none fst-normal link-light" asp-area="" asp-controller="Productos"
                            asp-action="Delete" asp-route-idProducto="@producto.IdProducto">Borrar</a>
                    </button>
                </td>
            </tr>
        }
    </tbody>
</table>

@* @using TiendaDB
@model List<Productos>
@{
    var ci = new System.Globalization.CultureInfo("es-AR");
}

<div class="container py-4">
  <div class="d-flex align-items-center justify-content-between mb-3">
    <div>
      <h1 class="h3 mb-0">Listado de Productos</h1>
      <p class="text-secondary small mb-0">Gestión de inventario</p>
    </div>
    <a class="btn btn-primary" asp-controller="Productos" asp-action="Create">
      <i class="bi bi-plus-lg"></i> Crear
    </a>
  </div>

  <div class="card shadow-sm">
    <div class="card-body p-0">
      <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
          <thead class="table-light"> 
            <tr>
              <th style="width: 90px;">ID</th>
              <th>Nombre</th>
              <th class="text-end" style="width: 180px;">Precio</th>
              <th class="text-end" style="width: 220px;">Acciones</th>
            </tr>
          </thead>
          <tbody>
          @if (Model == null || !Model.Any())
          {
            <tr>
              <td colspan="4" class="text-center text-secondary py-4">
                No hay productos aún.
              </td>
            </tr>
          }
          else
          {
            foreach (var producto in Model)
            {
              <tr>
                <td class="text-muted">@producto.IdProducto</td>
                <td class="fw-medium text-truncate" style="max-width: 420px;">
                  @producto.Descripcion
                </td>
                <td class="text-end fw-semibold">
                  @string.Format(ci, "{0:C}", producto.Precio)
                </td>
                <td class="text-end">
                  <div class="btn-group" role="group" aria-label="Acciones">
                    <a class="btn btn-outline-secondary btn-sm"
                       asp-controller="Productos"
                       asp-action="Edit"
                       asp-route-idProducto="@producto.IdProducto">
                      <i class="bi bi-pencil"></i> Editar
                    </a>

                    <form asp-controller="Productos"
                          asp-action="Delete"
                          asp-route-idProducto="@producto.IdProducto"
                          method="post"
                          class="d-inline">
                      @Html.AntiForgeryToken()
                      <button type="submit"
                              class="btn btn-outline-danger btn-sm"
                              onclick="return confirm('¿Borrar @producto.Descripcion?');">
                        <i class="bi bi-trash"></i> Borrar
                      </button>
                    </form>
                  </div>
                </td>
              </tr>
            }
          }
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
 *@